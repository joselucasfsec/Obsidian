### **Adicionar o tema kali dark nas opções**

sudo cp /usr/share/qtermwidget6/color-schemes/* /usr/share/qtermwidget5/color-schemes/

Quando explorar um PHP que possivelmente está executando comandos direto no terminal, pode ser que ele esteja utilizando a função eval() para executar esses comandos. Utilizando **acentos graves(`whoami`)** podemos executar qualquer comando diretamente no terminal

**DotGit** - Informa se a aplicação possui o diretório .git em produção